# Codex Installation Prompt

Paste the following to Codex:

```text
Fetch and follow the instructions from https://github.com/ZephyrCICD/ai-skill-git-commit-assistant/blob/main/.codex/INSTALL.md. If you cannot fetch that file directly, clone https://github.com/ZephyrCICD/ai-skill-git-commit-assistant into ~/ai/skills/ai-skill-git-commit-assistant, then run `codex plugin marketplace add ~/ai/skills/ai-skill-git-commit-assistant` and `codex plugin add git-commit-assistant@git-skills`. Verify the plugin is installed, then start a new Codex thread so the bundled `draft-commit-message` and `commit-changes` skills are available.
```

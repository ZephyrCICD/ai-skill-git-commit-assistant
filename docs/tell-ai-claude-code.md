# Tell AI for Claude Code

Copy this directly to an AI agent:

```text
Install the Git Commit Assistant skills repo for Claude Code by following docs/install-claude-code.md from https://github.com/ZephyrCICD/ai-skill-git-commit-assistant. Use the documented marketplace flow. After installation, verify that the Claude Code plugin `git-commit-assistant` is installed and that the skills `draft-commit-message` and `commit-changes` are available.
```
